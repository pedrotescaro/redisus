# ML

Esta pasta concentra a camada documental e de governança dos modelos.

## Estrutura

- `benchmarks/`: relatórios de baseline e comparação;
- `model_cards/`: documentação de cada modelo principal;
- `registry/`: inventário de modelos e artefatos.

## Observação

Os pesos continuam em `models/` por compatibilidade com o código atual. A próxima etapa recomendada é separar pesos grandes do repositório principal.
