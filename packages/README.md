# Packages

Esta pasta centraliza wrappers e módulos canônicos para a transição arquitetural.

Objetivo:

- expor imports estáveis para a nova estrutura;
- reduzir acoplamento direto com `src/` e arquivos de raiz;
- permitir migração incremental sem quebrar compatibilidade.
